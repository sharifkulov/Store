@model Store.Models.Category

@{
    ViewData["Title"] = "Добавление категории";
}

@{
    await Html.RenderPartialAsync("Partial/_nav_product");
}
<hr />
<br />


@using (Html.BeginForm("SaveCategory", "Administration"))
{
    @Html.AntiForgeryToken()

    @Html.ValidationSummary()
    <div class="form-row">
        <div class="form-group col-md-4">
            @Html.LabelFor(x => x.Name)
            @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
            @Html.ValidationMessageFor(x => x.Name)
        </div>
    </div>

    <br />
    if (Model.Name == null)
    {
        <button type="submit" class="btn btn-primary">Добавление новой категории</button>
    }
    else
    {
        @Html.HiddenFor(x=>x.Id)
        <button type="submit" class="btn btn-primary">Сохранить</button>

    }
}

<br />

<button type="button" class="btn btn-dark"
        onclick="location.href = '@Url.Action("Category", "Administration")'">
    Вернуться к списку
</button>