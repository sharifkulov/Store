@model IEnumerable<Store.Models.Brand>

@{
    ViewData["Title"] = "Бренд";
}


@{
    await Html.RenderPartialAsync("Partial/_nav_product");
}

<br />

<button type="button" class="btn btn-dark"
        onclick="location.href = '@Url.Action("AddBrand", "Administration")'">
    Добавить новый бренд
</button>
<br />
<br />


<table class="table table-striped table-condensed">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                Логотип
            </th>
            <th>
                Изменить
            </th>
            <th>
                Удалить
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <img src="~/images/@item.PhotoPath" alt="" height="30" />
            </td>
            <td>
                @Html.ActionLink("Изменить", "EditBrand", new { id = item.Id })
            </td>
            <td>
                @Html.ActionLink("Удалить", "DeleteBrand", new { id = item.Id })
            </td>
        </tr>
        }
    </tbody>
</table>
<br />
